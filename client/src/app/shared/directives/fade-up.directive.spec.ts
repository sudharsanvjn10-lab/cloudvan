import { FadeUpDirective } from './fade-up.directive';

describe('FadeUpDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeUpDirective();
    expect(directive).toBeTruthy();
  });
});
